{
  "name": "terraform-github-action",
  "version": "1.0.0",
  "description": "A github action that runs terraform apply and terraform plan on a given path for .tf files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && ncc build src -o dist"
  },
  "author": "Geodor Ruales",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@google-cloud/storage": "^6.12.0",
    "js-terraform": "^0.0.1"
  }
}
